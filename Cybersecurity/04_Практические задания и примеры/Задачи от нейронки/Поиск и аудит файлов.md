#lab #linux #cmd 

# 🧪 Лабораторная работа №5. Поиск и аудит файлов

> [!abstract] Сценарий
> Ты — специалист по ИБ, тебе нужно провести инвентаризацию конфигурационных файлов и найти потенциально опасные места в системе. Все действия выполняются без текстовых редакторов — только команды и перенаправления.

---

## 📋 Задание 1. Поиск недавно изменённых конфигураций

1. Найди все файлы с расширением `.conf` в каталоге `/etc`, которые были изменены за последние 7 дней.
2. Запиши их полные пути в файл `recent_configs.txt`.

**Мои команды:**
```bash
sudo find /etc -type f -name "*.conf" -mtime -7 > recent_configs.txt
```

В чем были трудности?
изначально искала ".conf" без * , потом поставила  *  после conf
после -mtime поставила 7, что означает 7 дней назад,  а не за последние 7 дней
[[find]]

**Что получилось:**
/etc/resolv.conf

---

## 📋 Задание 2. Поиск скриптов с открытыми правами

1. Найди все файлы с расширением `.sh` в домашнем каталоге и его подкаталогах, доступные для чтения всем пользователям (права `...r--` для «остальных»).
2. Сохрани их список вместе с правами доступа в файл `insecure_scripts.txt`.

**Мои команды:**
```bash
find ~ -type f -name "*.sh" -perm o=r > insecure_scripts.txt  
```
С чем были трудности:
забыла указать флаг -name
перепутала / и ~

**Что получилось:**
_Сколько скриптов найдено? Приведи пример строки из файла._

---

## 📋 Задание 3. Поиск SUID-программ

1. Найди все SUID-файлы во всей системе.
2. Подсчитай, сколько их всего, и запиши это число в файл `suid_count.txt` с пометкой «Total SUID files: N».

**Мои команды:**
```bash

```

**Что получилось:**
_Сколько SUID-файлов в системе?_